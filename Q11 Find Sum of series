#include <stdio.h>

int main() {
    float sum=0.0;
    int n;
    printf("Enter the value of n ");
    scanf("%d",&n);
    for(int i=1;i<=n;++i)
    {
       
        sum=sum+(1.0/(float)i);
        
    }
    printf("The sum is:%f",sum);
    return 0;
}
