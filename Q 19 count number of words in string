#include <stdio.h>

int main() {
 char str[300];
 printf("Enter the string");
 scanf("%[^\n]",str);
 int c=1;
 for(int i=0;i<300;++i)
 {
    if(str[i]==' ')
    ++c;
 }
 printf("the number of words are %d",c);
 printf("%s",str);
 
 return 0;
}
